#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

bool canTransform(char *s)
{
    for(int i=1;i<26;i++)
    {
        
    }
    return true;
}
int main()
{
    int length;
    char s[10000001];
    scanf("%d", &length);
    scanf("%s", s);
    if (canTransform(s))
        printf("I'm the god of Knuckles!\n");
    else
        printf("Different makes perfect\n");
}